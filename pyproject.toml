[project]
name = "kai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.1",
    "google-genai>=1.60.0",
    "pytest>=9.0.2",
    "python-dotenv>=1.2.1",
]

[tool.setuptools.packages.find]
where = ["."]
